{
  "name": "everything-gemini-cli",
  "version": "1.0.2",
  "description": "Agentic toolkit for comprehensive coding and development workflows.",
  "settings": [
    {
      "name": "Observer Enabled",
      "description": "Enable the background observer agent to analyze session observations",
      "envVar": "GEMINI_LEARNING_OBSERVER_ENABLED",
      "type": "boolean",
      "default": false
    },
    {
      "name": "Run Interval (Minutes)",
      "description": "How often the observer analyzes observations (in minutes)",
      "envVar": "GEMINI_LEARNING_RUN_INTERVAL",
      "type": "number",
      "default": 5
    },
    {
      "name": "Min Observations",
      "description": "Minimum observations before analysis runs",
      "envVar": "GEMINI_LEARNING_MIN_OBSERVATIONS",
      "type": "number",
      "default": 20
    },
    {
      "name": "Compact Threshold",
      "description": "Number of tool calls before suggesting a /compact",
      "envVar": "GEMINI_COMPACT_THRESHOLD",
      "type": "number",
      "default": 50
    },
    {
      "name": "GitHub Token",
      "description": "Personal Access Token for GitHub MCP server",
      "envVar": "GITHUB_TOKEN",
      "type": "string",
      "sensitive": true
    },
    {
      "name": "Tavily API Key",
      "description": "API Key for Tavily Search MCP server",
      "envVar": "TAVILY_API_KEY",
      "type": "string",
      "sensitive": true
    }
  ],
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "tavily": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.tavily.com/mcp/?tavilyApiKey=${TAVILY_API_KEY}"
      ]
    }
  }
}
